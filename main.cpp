#include <iostream>
#include <cmath>
using namespace std;

int main() {
  double a;

  cout << "Compute the specified expressions and print the output: " << endl;
  cout << "-------------------------------" << endl;
  
  a = (25.5 * 3.5 - 3.5 * 3.5) / (40.5 - 4.5);

  cout << "Result of the expression (25.5 * 3.5 - 3.5 * 3.5) / (40.5 - 4.5) is " << a << endl;

  return 0;
}